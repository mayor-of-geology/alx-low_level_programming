#include "main.h"

/**
 * _islower - checks if c is a lower case
 * @c: is the input value
 * Return: 1 if c is lower case. 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
